// const User = require('./users')
const Drawing = require('./drawings')

// User.hasMany(Order)
// Order.belongsTo(User)
// Drawing.belongsTo(User)
// User.hasMany(Drawing)

module.exports = {
  // User,
  Drawing
}
